{
  "name": "ins",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "preinstall": "apt-get update && apt-get install -y python3 python3-pip && pip3 install pycryptodome",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "description": "Implementation of 3DES for file encryption",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "multer": "^2.0.2"
  }
}
